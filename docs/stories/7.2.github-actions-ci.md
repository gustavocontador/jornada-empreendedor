# Story 7.2: GitHub Actions CI Pipeline

**Epic:** EPIC-007 - Setup Monorepo + CI/CD
**Status:** ✅ Approved
**Prioridade:** 🔴 P0 (Bloqueante)
**Estimativa:** 2-3 dias
**Created:** 2026-03-15
**Owner:** @sm (River)
**Validated:** 2026-03-15 by @po (Pax)

---

## Executor Assignment

**executor:** "@devops"
**quality_gate:** "@architect"
**quality_gate_tools:** ["CodeRabbit Pre-PR", "CI Pipeline Validation"]

**Work Type Justification:**
- **Infra/CI/CD/Deploy** → Primary executor: @devops
- Quality gate by @architect to validate pipeline architecture and quality gates

---

## Story

**As a** developer,
**I want** automated CI pipeline with linting, testing, and quality gates,
**so that** code quality is verified before merge and broken code never reaches production.

---

## Acceptance Criteria

1. [ ] `.github/workflows/ci.yml` criado
2. [ ] Jobs paralelos configurados: lint-backend, lint-frontend, test-backend, test-frontend
3. [ ] Matrix builds: Python 3.12, Node 20
4. [ ] Quality gate: test coverage > 80% (fail se não atingir)
5. [ ] Quality gate: lint pass (Ruff backend, ESLint frontend)
6. [ ] Quality gate: typecheck pass (mypy backend, tsc frontend)
7. [ ] PR bloqueado automaticamente se qualquer job falhar
8. [ ] Status badges no README.md
9. [ ] CI runtime < 5min (p95)
10. [ ] Cache de dependências funcionando (pip cache, npm cache)

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Deployment (CI/CD Pipeline)
**Secondary Type(s):** Architecture (Quality Gates)
**Complexity:** Medium-High

### Specialized Agent Assignment

**Primary Agents:**
- @devops (executor)
- @architect (quality gate)

**Supporting Agents:**
- @qa (test coverage validation)

### Quality Gate Tasks

- [ ] Pre-Commit (@devops): Run before marking story complete
- [ ] Pre-PR (@architect): Validate CI pipeline structure
- [ ] Test Run: Execute CI pipeline em PR de teste

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @devops (check mode)
- Max Iterations: N/A
- Timeout: N/A
- Severity Filter: CRITICAL

**Predicted Behavior:**
- CRITICAL issues: report_only
- HIGH issues: report_only

### CodeRabbit Focus Areas

**Primary Focus:**
- CI/CD pipeline: YAML syntax validation
- Quality gates: Coverage thresholds enforced
- Security: No secrets in workflow files

**Secondary Focus:**
- Performance: Job parallelization
- Caching: Dependency cache hit rate

---

## Tasks / Subtasks

### Task 1: Criar Workflow CI Base (AC: 1, 2, 3)
- [ ] Criar `.github/workflows/ci.yml`
- [ ] Configurar triggers:
  ```yaml
  on:
    push:
      branches: [main, develop]
    pull_request:
      branches: [main, develop]
  ```
- [ ] Definir variáveis de ambiente globais:
  ```yaml
  env:
    PYTHON_VERSION: '3.12'
    NODE_VERSION: '20'
  ```
- [ ] Configurar matrix strategy para Python 3.12 e Node 20

### Task 2: Job Lint Backend (AC: 2, 5)
- [ ] Criar job `lint-backend`:
  ```yaml
  lint-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      - run: pip install -r backend/requirements-dev.txt
      - run: ruff check backend/
      - run: black --check backend/
  ```
- [ ] Adicionar cache de pip para performance

### Task 3: Job Lint Frontend (AC: 2, 5)
- [ ] Criar job `lint-frontend`:
  ```yaml
  lint-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci --prefix frontend
      - run: npm run lint --prefix frontend
  ```
- [ ] Adicionar cache de npm para performance

### Task 4: Job Test Backend (AC: 2, 4, 6)
- [ ] Criar job `test-backend` com dependência em `lint-backend`
- [ ] Configurar PostgreSQL service container:
  ```yaml
  services:
    postgres:
      image: postgres:16
      env:
        POSTGRES_USER: test
        POSTGRES_PASSWORD: test
        POSTGRES_DB: test_db
      options: >-
        --health-cmd pg_isready
        --health-interval 10s
      ports:
        - 5432:5432
  ```
- [ ] Executar pytest com coverage:
  ```bash
  pytest --cov=app --cov-report=xml --cov-report=term
  ```
- [ ] Validar coverage > 80% (fail se menor)
- [ ] Upload coverage para Codecov
- [ ] Executar mypy (typecheck)

### Task 5: Job Test Frontend (AC: 2, 4, 6)
- [ ] Criar job `test-frontend` com dependência em `lint-frontend`
- [ ] Executar Jest com coverage:
  ```bash
  npm run test -- --coverage
  ```
- [ ] Validar coverage > 80% (fail se menor)
- [ ] Upload coverage para Codecov
- [ ] Executar tsc (typecheck):
  ```bash
  npm run typecheck
  ```

### Task 6: Quality Gate - Coverage Threshold (AC: 4)
- [ ] Adicionar step de validação de coverage:
  ```bash
  BACKEND_COVERAGE=$(cat backend/coverage.xml | grep -oP 'line-rate="\K[^"]+' | head -1)
  if (( $(echo "$BACKEND_COVERAGE < 0.80" | bc -l) )); then
    echo "❌ Backend coverage below 80%"
    exit 1
  fi
  ```
- [ ] Repetir para frontend
- [ ] Falhar job se threshold não atingido

### Task 7: Branch Protection Rules (AC: 7)
- [ ] Configurar branch protection no GitHub:
  - [ ] Require status checks to pass before merging
  - [ ] Require branches to be up to date before merging
  - [ ] Required checks: lint-backend, lint-frontend, test-backend, test-frontend
  - [ ] Do not allow bypassing the above settings
- [ ] Testar: criar PR com código que falha lint → deve bloquear merge

### Task 8: Status Badges e Documentação (AC: 8)
- [ ] Adicionar badges no README.md:
  ```markdown
  [![CI](https://github.com/USER/REPO/workflows/CI/badge.svg)](https://github.com/USER/REPO/actions)
  [![codecov](https://codecov.io/gh/USER/REPO/branch/main/graph/badge.svg)](https://codecov.io/gh/USER/REPO)
  ```
- [ ] Documentar como rodar CI localmente (usando `act` ou scripts)

### Task 9: Performance Optimization (AC: 9, 10)
- [ ] Adicionar cache de pip:
  ```yaml
  - uses: actions/cache@v4
    with:
      path: ~/.cache/pip
      key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
  ```
- [ ] Adicionar cache de npm:
  ```yaml
  - uses: actions/cache@v4
    with:
      path: ~/.npm
      key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
  ```
- [ ] Validar runtime < 5min executando CI completo

### Task 10: Validação Final
- [ ] Criar PR de teste com código válido → CI deve passar
- [ ] Criar PR de teste com lint error → CI deve falhar
- [ ] Criar PR de teste com test failure → CI deve falhar
- [ ] Criar PR de teste com coverage < 80% → CI deve falhar
- [ ] Verificar que merge está bloqueado quando CI falha

---

## Dev Notes

### Contexto do Projeto

O CI/CD é crítico para manter qualidade desde o dia 1. Este pipeline garante:
- **Zero broken builds:** Código quebrado nunca chega na main
- **Test coverage:** 80%+ obrigatório em backend e frontend
- **Lint enforcement:** Ruff (backend), ESLint (frontend)
- **Type safety:** mypy (backend), tsc (frontend)

### Arquitetura do Pipeline

```
CI Workflow (.github/workflows/ci.yml)
├── lint-backend (Python 3.12)
│   ├── Ruff (linting)
│   └── Black (formatting check)
├── lint-frontend (Node 20)
│   └── ESLint
├── test-backend (depends on lint-backend)
│   ├── PostgreSQL service container
│   ├── pytest + coverage
│   ├── mypy typecheck
│   └── Coverage threshold check (80%)
└── test-frontend (depends on lint-frontend)
    ├── Jest + coverage
    ├── tsc typecheck
    └── Coverage threshold check (80%)
```

### Referências Técnicas

- **GitHub Actions Docs:** https://docs.github.com/en/actions
- **Matrix Builds:** https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs
- **Codecov:** https://codecov.io/
- **Branch Protection:** https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches

### Integração com Monorepo (Story 7.1)

Depende de Story 7.1 estar completa:
- Workspaces configurados (`backend/`, `frontend/`, `shared/`)
- `turbo.json` com pipelines definidos
- Scripts `turbo run lint`, `turbo run test` funcionando

### Quality Gates (conforme PRD)

PRD v2.1.0 especifica:
- **Test Coverage:** 80%+ (backend + frontend)
- **Observabilidade:** Sentry ativo desde dia 1 (já implementado na Fase 1)
- **CI/CD:** Desde dia 1 (este story implementa)

---

## Testing

### Testing Standards

**Framework:**
- Backend: pytest + pytest-cov
- Frontend: Jest + @testing-library/react

**Test Coverage:**
- Minimum: 80%
- Enforced by CI quality gate

**Validation for this Story:**
- [ ] CI workflow YAML é válido (GitHub Actions syntax check)
- [ ] Todos os jobs executam com sucesso em PR de teste
- [ ] Branch protection bloqueia merge quando CI falha
- [ ] Cache de dependências reduz runtime em pelo menos 30%
- [ ] Coverage reports são gerados corretamente

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 1.0.0 | Story inicial criada | @sm (River) |
| 2026-03-15 | 1.0.1 | Story validada (10/10) - Aprovada para desenvolvimento | @po (Pax) |

---

## Dev Agent Record

*(This section will be populated by @devops during implementation)*

---

**Created by:** @sm (River)
**Date:** 2026-03-15
**Epic:** EPIC-007

— River, removendo obstáculos 🌊
